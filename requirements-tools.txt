# Requirements for standalone YOLOv11 people detector
# Install with: pip install -r requirements-tools.txt

ultralytics>=8.0.0
opencv-python>=4.5.0
numpy>=1.21.0,<2.0  # Pin to <2.0 if NumPy 2.0 causes conflicts
fastapi>=0.110
uvicorn[standard]>=0.22

# Optional: For GPU acceleration (if CUDA available)
# torch>=1.11.0
# torchvision>=0.12.0
